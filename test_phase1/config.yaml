max_iterations: 3

steps:
  - role: coder
    agent: primary_coder
  - role: reviewer
    agent: strict_reviewer

agent_configs:
  primary_coder:
    style: google
  strict_reviewer:
    require_docstring: true
    max_line_length: 100
    rules:
      - no_todo
